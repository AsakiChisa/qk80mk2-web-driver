# 更新日志

本文件记录 QK80 MK2 Web Driver 的主要版本变化。

## v1.8.0 — V5 持久静态底图与按键动画共存（2026-09-15）

- 发布 `PERSISTENT_EFFECT_V5_BACKGROUND` PLC 固件，基于已实机验证的 V4 构建。
- 保存时把当前完整 91 灯 RGB888 画板与呼吸/流动渐变参数一同写入专用 Flash 页。
- 重启或重新上电后先恢复各键静态底色，再以约 31 FPS 只更新动画选中灯。
- 网页“保存到键盘”会先安全提交完整 RAM 底图，再发送持久保存命令。
- V5 记录扩展为 300 bytes，并继续采用正文先写、校验标记最后写入的掉电保护策略。
- V5 已通过离线代码区、记录损坏拒绝、UF2 完整性验证及实机验收。
- 发布页补充配套 Master RAM_FRAME_V2 下载和哈希校验，首次安装明确按 Master V2 → PLC V5 顺序分别升级。

## v1.7.5 — 顺滑效果支持静态画板背景（2026-09-14）

- 启动顺滑呼吸/渐变前，自动把当前完整 91 灯画板原子写入 RAM。
- 后续 30 FPS `MASK_COLOR` 只更新选中动画灯，未选中灯保留各自静态颜色。
- 顺滑播放期间阻止画笔修改并提示停止后编辑，避免网页与硬件画面不一致。
- 明确 V4 断电保存仍只保存动画参数，尚不包含完整静态背景帧。

## v1.7.4 — 导航区实际布局修正（2026-09-14）

- 修正右上导航区下排左右矩阵位置，实际顺序为 `Delete / End / Vol-`。
- 改键基础键盘图中的 `PgUp / PgDn` 替换为本机实际的 `Vol+ / Vol-`。
- 矩阵坐标修正后仍保持三颗物理灯从左到右对应 LED `29 / 30 / 31`。

## v1.7.3 — RGB 键帽显示实际键值（2026-09-14）

- 主键盘 RGB 页面在连接并读取键盘后，显示当前读取 Layer 的实际 keycode 名称。
- 在改键页切换 Layer、写入键值、撤销、重做或恢复默认键位后，RGB 页面标签同步刷新。
- 未连接或尚未读取 keymap 时，保留物理布局名称作为回退显示。

## v1.7.2 — 修正 QK 官方驱动入口（2026-09-14）

- 将网页内所有官方驱动入口统一修正为 `https://cfg.qwertykeys.com/`。
- 修正固件升级按钮打开的目标地址，并移除旧错误域名。

## v1.7.1 — 固件升级改用官方驱动（2026-09-14）

- 根据实机反馈停用浏览器“另存为到 UF2 引导盘”的自制写入流程，该流程存在升级兼容问题。
- 固件页简化为“选择最新版 V4 → 下载并打开 QK 官方驱动”，只保留本地 UF2 完整性校验。
- 移除本地固件选择、引导盘另存为、勾选框和手动输入 `FLASH PLC/MASTER` 的复杂界面。
- 实际刷写统一由已实机成功的 QK 官方驱动完成；本网页不会直接写入键盘。
- 记录 V4 已通过官方驱动刷入且持久灯效保存功能正常。

## v1.7.0 — 键盘本地持久灯效候选版（2026-09-14）

- 新增 `PERSISTENT_EFFECT_V4` PLC 候选固件，支持把选中按键的呼吸或流动渐变参数保存到键盘专用 Flash 页。
- 保存后由 PLC 本地约 31 FPS 渲染，关闭网页及断电重启后仍可自动运行。
- 新增“保存到键盘（断电保留）”和“清除键盘本地灯效”，每次明确操作只擦写一次，播放过程不反复写 Flash。
- 网页 RAM 整帧或 MASK_COLOR 接管期间会暂停持久引擎，EXIT 后自动恢复本地灯效。
- Backspace、Enter、反斜杠、Shift、Space、RAlt 等多灯按键按同一空间相位渲染。
- 持久记录使用数据先写、校验标记最后写的掉电保护，并拒绝非法掩码、flags、周期和校验值。
- V4 已通过离线构建与结构验证，尚未完成实机验收；自制固件异常时应刷回官方固件。

## v1.6.0 — 安全 UF2 刷写与 RGB 布局整理（2026-09-14）

- 新增独立“固件升级”页，支持内置 RAM_FRAME_V3 PLC 固件与本地 Master / PLC UF2。
- 写入前强制检查 UF2 魔数、512-byte 块、256-byte payload、块序号、Family ID、地址范围与 SHA-256。
- 只接受 QK80 MK2 Master `0x514B4D02` 和 PLC `0x514B4D50`，其他 Family ID 直接拒绝。
- 写入前要求保留官方固件、勾选风险确认并输入 `FLASH PLC` / `FLASH MASTER`。
- 通过浏览器文件保存接口由用户手动选择 UF2 引导盘；不支持时退化为下载后手动复制。
- 页面明确警告自制固件异常时刷回官方固件，并提供 Qwertykeys 官方下载、升级教程和 QK HUB 入口。
- 主键盘 RGB 页的“动画方案”保留在右侧控制栏；“主键盘 RGB 通道状态”移到页面最底部并默认收起。
- 动画帧缩略图列表和 LED 物理映射改放到键盘画布正下方，用横向多列填充原有空白区域。
- 自定义效果选键支持按下后滑动连续选择；从未选中键开始为连续加选，从已选中键开始为连续取消。
- 逐键画笔统一使用左键：从熄灭键开始点击或拖动连续上色，从已亮键开始连续熄灭，不再依赖右键擦除。

## v1.5.0 — 按键级顺滑效果（2026-09-14）

- 效果生成器支持在键盘图上选择一个或多个作用按键，未选中的键不再被整帧替换。
- 宽键自动覆盖其全部物理 LED，例如 Backspace 的 45/46/47。
- 新增 RAM_FRAME_V3 `MASK_COLOR`：单包携带 91-bit 灯掩码和 RGB，只刷新被选中的灯。
- 新增按键级呼吸和双色连续渐变播放；实机稳定达到 30 FPS，无闪烁、卡顿或超时。
- V3 PLC 候选固件保持 V2 Master 和原整帧协议兼容，不写 EEPROM，EXIT/拔插后恢复官方灯效。
- 已验证 Esc 单灯隔离、Backspace 45/46/47 三灯同步、正常键盘输入、停止播放和官方灯效恢复。

## v1.4.0 — RGB 呼吸与流动渐变（2026-09-14）

- 主键盘 RGB 页面新增“自定义效果生成器”。
- 新增呼吸灯：自选颜色、0/10/25/50% 最低亮度以及 8/12/16/24 帧循环。
- 新增双色流动渐变：支持从左到右、从上到下、对角线和中心扩散方向。
- 使用余弦平滑曲线生成首尾连续的循环帧。
- 生成结果直接进入现有多帧编辑器，可预览、手动修改、保存方案、导出 JSON 或通过 RAM_FRAME_V2 实时播放。

## v1.3.1 — 视频上传内存修复（2026-09-13）

- 修复长视频传输时逐包调试日志持续增长，最终导致浏览器 `Out of Memory` 的问题。
- 屏幕文件数据改为复用固定 64-byte Buffer 流式发送，避免每个 56-byte 分片产生多组临时数组。
- 屏幕文件数据只记录开始和完成摘要，不再把近百万个 E1 分片写入页面日志。
- 上传进度改为按百分比更新，避免高频 DOM 重绘。
- 全局调试日志增加容量上限，防止长时间运行后无限增长。

## v1.3.0 — 彩色屏幕素材（2026-09-13）

- 新增独立的“彩色屏幕”页面，与 7×7 点阵明确分离。
- 支持 PNG、JPG、BMP 图片上传。
- 支持 GIF 动画与 H.264 MP4 视频，网页端可选择 5–30 FPS 采样，最多 500 帧。
- 支持多选图片或选择文件夹生成相册，可设置 5/10/15/30 秒停留与五种切换方向。
- 图片、动画和相册都可写入自定义槽或当前主题槽。
- 素材完全在浏览器本地裁剪并转换为 320×172 RGB565，不上传网络。
- 使用官方固件已有的 CDC `E0/E1/E2` 文件链，56-byte 分片并支持逐块 ACK 与取消。
- 屏幕素材为键盘本地持久化内容，网页关闭后仍可独立显示；写入前明确提示将覆盖目标槽。
- 固件升级 `EF/F0/F1` 保持隔离，不在屏幕页面暴露。

## v1.2.6 — 正式稳定版（2026-09-13）

### 主键盘 RGB

- 将实机验证通过的 10 ms 包间隔、4 FPS 设置为默认稳定高速档。
- 保留 1 FPS 保守档。
- 移除冷启动不稳定的 5 ms 档和会导致传输停滞的 0 ms 档。
- 实时动画改为完整帧顺序播放，不再为了追赶时间线跳过动画帧。
- 单帧传输超过目标周期时只记录“超周期帧”，并按设备实际吞吐自然降速。
- 传输超时或失败时自动尝试发送 EXIT，恢复官方灯效。

### 发布整理

- 精简 README，移除已经失效或容易与当前行为混淆的旧版说明。
- 更新 RAM_FRAME_V2 协议文档和安全边界。
- 清理本地 Python 环境、逆向中间产物、分析脚本及本地助手配置。
- 项目保持原生 HTML、CSS、JavaScript，无 npm 依赖。

### 最终实机结果

- 冷启动后立即播放。
- 实际帧率：4 FPS。
- 超周期帧：0。
- 无灯效残留、串色或异常断连。
- 可以正常退出接管并恢复官方灯效。

## v1.2.5 — 冷启动稳定性测试

- 增加 10 ms 包间隔、约 4 FPS 的冷启动测试档。
- 不使用预热帧，直接验证首次上电后的真实启动表现。
- 保留 5 ms 档作为不稳定对照。
- 实机确认 10 ms 档能够从第一帧立即运动，实际 4 FPS、超周期帧 0。

## v1.2.4 — 顺序播放调度

- 删除追赶时间线和自动跳帧逻辑。
- 每帧完成 COMMIT 后再安排下一帧。
- 当链路达不到目标 FPS 时自然降低速度，避免启动阶段连续补发造成卡顿。
- 停止播放后不再额外发送下一帧。

## v1.2.3 — 撤回零等待模式

- 实机确认 0 ms 额外间隔会让传输从首帧开始停滞。
- 从界面和配置恢复逻辑中移除 0 ms 模式。
- 增加实时传输错误后的自动 EXIT 恢复。

## v1.2.2 — 0 ms 极限实验

- 曾增加仍等待 HID 回显、但取消额外休眠的 0 ms 实验档。
- 该方案实机验证失败，已在 v1.2.3 撤回。

## v1.2.1 — 10 FPS 实验

- 增加目标 10 FPS、5 ms 包间隔的实验档。
- 实测没有灯效残留，可以恢复官方灯效。
- 实际吞吐只有约 5–6 FPS，并出现启动卡顿和跳帧。
- 长时间冷启动测试发现约需一分钟才逐渐正常，因此未进入正式版。

## v1.2.0 — RAM_FRAME_V2 接入

### 协议

- 主键盘 RGB 从不可用的标准 VIA Per-Key 路径迁移到私有 `RAM_FRAME_V2`。
- Host 路径固定为 Raw HID `07 16 05`，PLC 私有命令为 `0x5A`。
- 一帧包含 91 颗灯、273 bytes RGB888 数据。
- 每帧采用 `BEGIN → 13 DATA → COMMIT`。
- 每个 DATA 包携带 21 bytes，即 7 颗灯。
- 加入 version、session、sequence、长度和 CRC-8/ATM 校验。
- DATA 仅更新软件缓冲，COMMIT 时才进行一次物理刷新。

### 安全与恢复

- 所有自定义 RGB 颜色只写 RAM，不调用 RGB `CUSTOM_SAVE`，不写 EEPROM。
- 增加“退出接管 / 恢复官方灯效”按钮。
- 页面关闭或刷新时尽力自动发送 EXIT。
- HID 断开或重新上电后恢复官方灯效。

### LED 映射

- 固化 LED 0–90 的 91/91 实机映射。
- Backspace、反斜杠、Enter、左 Shift、Space 和右 Alt 等宽键自动控制对应的多颗灯。
- 黑色会作为真正的 `[0,0,0]` 写入，不再只是网页预览状态。

### 实机验证

- 91 灯暗红与全黑整帧完整，无残留。
- 红、绿、蓝通道正确，无串色。
- DATA 上传期间保持旧画面，COMMIT 时一次切换。
- 错误 sequence 不改变物理画面。
- 新 session 可以从失败传输中恢复。
- EXIT、刷新、关闭页面和重新上电均可恢复官方灯效。

## v1.1.1 — RGB 协议诊断

- 增加 VIA、Layer、CDC、轴灯参数和标准 Per-Key 接口诊断。
- 确认原固件未开放标准 VIA Per-Key `00 01` 接口。
- 增加诊断 JSON 导出及可选 D1 Matrix RAM 探针。
- 为后续私有主键盘 RGB 通道逆向提供证据。

## v1.1.0 — RGB 动画编辑增强

- 增加 RGB 帧拖拽排序。
- 增加网页预览、硬件播放统计和本地动画方案。
- 支持 RGB 项目的 JSON 导入与导出。
- 串行化 HID 命令，减少并发请求造成的响应错配。

## v1.0.x — 正式网页驱动基础

- 完成键位、宏、普通灯光、7×7 点阵、Profile 和设备设置页面。
- 增加主键盘 RGB 画板和多帧编辑界面。
- 增加响应式布局、本地主题和背景设置。
- 提供 Windows 本地 HTTP 启动脚本。
